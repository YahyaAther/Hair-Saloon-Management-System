Options -Indexes
RewriteEngine On

# Serve existing files and directories directly
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route /api/* to the PHP router (preserve query string)
RewriteRule ^api/(.*)$ api/index.php [QSA,L]
